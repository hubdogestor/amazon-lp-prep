Olá! Quero usar toda a SUA capacidade de revisão e escrita para aprimorar meus cases de entrevista da Amazon.

- Estou anexando o arquivo de instruções chamado **MASTER_REVIEW – Revisão Completa dos Cases**, que define exatamente como desejo que a revisão seja conduzida. (MASTER_REVIEW.txt).

- Além disso, após cada revisão, quero validações de formato/encoding para evitar caracteres invisíveis ou tipográficos problemáticos.

--------------------------------------------------

# Instruções Base (permanentes nessa conversa):

1) Assimile integralmente o conteúdo do arquivo “MASTER_REVIEW – Revisão Completa dos Cases (.js)” (regras de STAR(L), FUPs, estilo Amazon Bar Raiser, checklist). Siga-o à risca. [IMPORTANTE]

2) Ao receber um case .js, faça a revisão completa segundo o MASTER_REVIEW, mantendo campos e estrutura originais (id, title, company, period, isTopCase, isGoodCase; blocos pt/en com s,t,a,r,l; array fups com {q,a,q_en,a_en}). 

3) O resultado deve ser o ARQUIVO .JS COMPLETO E SUBSTITUÍVEL — nada de comentários fora do código. 

--------------------------------------------------

# Requisitos de formatação/encoding (obrigatórios):

- Codificação: **UTF-8 sem BOM**.

- Conteúdo: **sem caracteres não-imprimíveis** (ex.: U+200B..U+200D, U+FEFF), **sem espaços não-quebráveis** (U+00A0), **sem aspas tipográficas** (“ ” ‘ ’). Use aspas retas " '.

- **Sem tabs**: converta tabs em 2 espaços. **Sem trailing spaces**. Quebra de linha **LF (\n)** uniforme.

- **Somente ASCII visível** para símbolos e pontuação. (Se houver acentos PT-BR, mantenha-os como UTF-8 padrão, mas não insira variantes tipográficas.)
- Não alterar nomes de chaves/campos nem a assinatura do módulo (`export default ...`).

--------------------------------------------------

# Validações que você deve executar ANTES de devolver:

1) **Lint mínimo textual** (descrever resultado):
   - Remover/recusar: [\u200B-\u200D\uFEFF\u00A0] e aspas tipográficas.
   - Normalizar aspas para " / ' e hífens para o simples '-'.
   - Normalizar quebras para LF e remover espaços ao fim de linha.

2) **Varredura de caracteres fora de ASCII visível** (exibir lista se houver; acentos PT-BR são OK, mas confirme que não há tipografia “curly”).
   - Regex de auditoria sugerida: /[^\x09\x0A\x0D\x20-\x7E\u00C0-\u017F]/  (permite ASCII visível + faixa latina básica/estendida para acentos).

3) **Sanidade estrutural**:
   - Existe objeto raiz com as chaves: id, title, company, period, isTopCase, isGoodCase, pt:{s,t,a,r,l}, en:{s,t,a,r,l}, fups:[{q,a,q_en,a_en}, …]?
   - `export default <nome_da_constante>` presente?
   - fups TODAS bilíngues e coerentes com o STAR(L)?

4) **Autochecagem JS**:
   - Gere internamente um `JSON.stringify` do “equivalente” (sem a linha de export e funções) apenas para testar **parseabilidade** (não devolva o JSON; é só checagem).
   - Confirme que as strings não contêm backticks abertos, barras invertidas inválidas ou aspas não fechadas.

--------------------------------------------------

# Entrega (formato da resposta):

1) **Resumo de validações** (4–8 linhas, bullets), indicando se houve correções de encoding/aspas/whitespace/tabs.

2) Em seguida, **apenas um único bloco de código** contendo o arquivo .js final, pronto para substituir o original. Use:
   ```javascript
   // <nome-do-arquivo>.js
   <código completo aqui>
   ```

3) Nenhuma explicação após o bloco de código.

--------------------------------------------------

# Critérios de revisão de conteúdo (MASTER_REVIEW):

Critérios de revisão de conteúdo (MASTER_REVIEW)
- Aplique integralmente o MASTER_REVIEW: 
  • Revisão do STAR(L) com dados, comparativos antes/depois, trade-offs e ownership;
  • 6 FUPs centrais obrigatórias + complementares coerentes, bilíngues, entre 2–4 linhas, tom executivo;
  • Não há limite para a quantidade total de FUPs;
  • Estilo Amazon Bar Raiser, sem citar nomes dos princípios.
- Não resuma nem corte: prefiro detalhamento e fidelidade aos fatos.
- Mantenha o arquivo bilíngue e os nomes de chaves exatamente como estão.

--------------------------------------------------

# Observações finais:
- Se algo impedir a conformidade (ex.: chave faltando), devolva primeiro um **Resumo de validações** explicando o bloqueio e só então o arquivo ajustado (se possível).
- Quando estiver pronto para começar, responda: **“✅ Pronto para revisar cases com validações.”**